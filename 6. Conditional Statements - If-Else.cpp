#include<iostream>

using namespace std;

int main()
{
	int a = 40, b = 50;
	
	if (b<a)
	//Usually if function comes with (condition) which is written in brackets.
	{
		cout<<"B is greater than A.";
	}
	else
	{
		cout<<"B is smaller than A.";
	}
	
	return 0;
}
